PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT ?graph ?class ?label ?count
WHERE
{
    {
        select ?graph ?class (count(?class) as ?count)  
        where {
            graph ?graph {
                [] a ?class .
            }
        }
        group by ?graph ?class
        order by desc(?count)
    }
    
    optional {
        ?class rdfs:label ?label .
    }
}
